# The ULTIMATE Conf

This is mine ULTIMATE nvim conf, adapt for my usage. Feel free to use, but not so much to commit. Not being rude, but it's my config, it's my workflow.

## How can I use it? 

```bash
git clone https://github.com/guztaver/the-ultimate-conf.nvim/
mv ~/.config/nvim ~/.config/nvim.bak
mv the-ultimate-conf.nvim ~/.config/nvim
```
